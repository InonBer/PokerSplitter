# i18n & RTL Support Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add English/Hebrew internationalization with full RTL layout support to PokerSplitter.

**Architecture:** i18next + react-i18next for translations, expo-localization for device locale detection, react-native-restart for app restart after RTL toggle. Translation keys stored in JSON files, language preference persisted in MMKV, RTL controlled via I18nManager.

**Tech Stack:** i18next, react-i18next, expo-localization, react-native-restart, react-native-mmkv (existing)

**Spec:** `docs/superpowers/specs/2026-04-07-i18n-rtl-design.md`

---

## Chunk 1: Foundation — Dependencies, i18n Init, Translation Files

### Task 1: Install Dependencies

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Install i18n packages**

```bash
npx expo install expo-localization && npm install i18next react-i18next react-native-restart
```

- [ ] **Step 2: Verify installation**

```bash
node -e "require('i18next'); require('react-i18next'); console.log('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add i18next, react-i18next, expo-localization, react-native-restart"
```

---

### Task 2: Create English Translation File

**Files:**
- Create: `src/i18n/locales/en.json`

- [ ] **Step 1: Create the English translations file**

Create `src/i18n/locales/en.json` with all translation keys extracted from the codebase. Use flat namespace with screen prefixes. Reference every screen and component file to extract all user-facing strings.

Key sources and their strings:

**common** (shared across screens):
- `common.arrow`: `"→"` (directional arrow for transfers)
- `common.ok`: `"OK"`
- `common.cancel`: `"Cancel"`
- `common.confirm`: `"Confirm"`
- `common.done`: `"Done"`
- `common.save`: `"Save"`
- `common.delete`: `"Delete"`

**nav** (navigation titles — used by useTranslatedTitle hook):
- `nav.home`: `"PokerSplitter"`
- `nav.gameSetup`: `"New Game"`
- `nav.activeGame`: `"Game"`
- `nav.finalChipCount`: `"Chip Count"`
- `nav.settlement`: `"Settlement"`
- `nav.gameDetail`: `"Game Detail"`
- `nav.stats`: `"All-Time Stats"`
- `nav.settings`: `"Settings"`
- `nav.contacts`: `"Contacts"`

**home** (HomeScreen.tsx):
- `home.newGame`: `"+ New Game"`
- `home.noGames`: `"No games yet. Start one!"`
- `home.gamesUsed`: `"{{count}}/{{limit}} games used"`
- `home.gamesUsedMax`: `"{{limit}}/{{limit}} games used — upgrade to add more"`
- `home.players_one`: `"{{count}} player"`
- `home.players_other`: `"{{count}} players"`
- `home.active`: `"Active"`
- `home.finished`: `"Finished"`
- `home.stats`: `"Stats"`

**setup** (GameSetupScreen.tsx):
- `setup.gameName`: `"Game name (optional)"`
- `setup.playerName`: `"Player name"`
- `setup.buyIn`: `"Buy-in $"`
- `setup.addPlayer`: `"+ Add Player"`
- `setup.startGame`: `"Start Game"`
- `setup.needPlayers`: `"Need at least 2 players"`
- `setup.needNames`: `"All players need a name"`
- `setup.needBuyIn`: `"All buy-in amounts must be positive numbers"`
- `setup.duplicateNames`: `"Duplicate names"`
- `setup.duplicateNamesMsg`: `"All players must have unique names"`

**activeGame** (ActiveGameScreen.tsx):
- `activeGame.pot`: `"Pot: ${{amount}}"`
- `activeGame.rebuy`: `"Rebuy"`
- `activeGame.cashOut`: `"Cash Out"`
- `activeGame.endGame`: `"End Game"`
- `activeGame.endGameConfirm`: `"Are you sure you want to end the game?"`
- `activeGame.enterAmount`: `"Enter amount:"`
- `activeGame.invalidAmount`: `"Enter a valid positive amount"`

**finalChipCount** (FinalChipCountScreen.tsx):
- `finalChipCount.expectedTotal`: `"Expected total: ${{amount}}"`
- `finalChipCount.chipsPlaceholder`: `"Chips $"`
- `finalChipCount.calculate`: `"Calculate Settlement"`
- `finalChipCount.enterChipCounts`: `"Enter chip counts"`
- `finalChipCount.missingAmount`: `"Missing or invalid amount for {{name}}"`
- `finalChipCount.mismatch`: `"Chip count mismatch"`
- `finalChipCount.mismatchMsg`: `"Total entered: ${{entered}}\nExpected: ${{expected}}\n\nPlease recount and try again."`

**settlement** (SettlementScreen.tsx):
- `settlement.noTransfersNeeded`: `"No transfers needed!"`
- `settlement.everyoneBrokeEven`: `"Everyone broke even."`
- `settlement.totalPot`: `"Total pot ${{amount}}"`
- `settlement.transferCount_one`: `"{{count}} transfer · Total pot ${{pot}}"`
- `settlement.transferCount_other`: `"{{count}} transfers · Total pot ${{pot}}"`
- `settlement.shareResults`: `"Share Results"`
- `settlement.shareWhatsApp`: `"Share to WhatsApp"`
- `settlement.messagePlayers`: `"Message Players"`
- `settlement.whatsappNotInstalled`: `"WhatsApp not installed"`
- `settlement.owes`: `"{{from}} owes {{to}} ${{amount}}"`
- `settlement.sendWhatsApp`: `"Send on WhatsApp"`
- `settlement.contactHint`: `"Pick {{name}} from Contacts when starting a game to enable this"`
- `settlement.shareTitle`: `"Poker Settlement"`
- `settlement.shareTransferLine`: `"{{from}} {{arrow}} {{to}}: ${{amount}}"`
- `settlement.noTransfersShareLine`: `"No transfers needed — everyone broke even!"`

**paywall** (PaywallScreen.tsx):
- `paywall.title`: `"Unlock PokerSplitter Pro"`
- `paywall.subtitle`: `"One-time purchase · No subscription"`
- `paywall.feature.unlimitedGames`: `"Unlimited games"`
- `paywall.feature.contacts`: `"Save player contacts"`
- `paywall.feature.whatsapp`: `"WhatsApp integration"`
- `paywall.feature.stats`: `"All-time player stats"`
- `paywall.feature.csv`: `"CSV export"`
- `paywall.feature.backup`: `"Backup & restore"`
- `paywall.processing`: `"Processing…"`
- `paywall.unlock`: `"Unlock Pro — $2.99"`
- `paywall.restore`: `"Restore Purchase"`
- `paywall.purchaseFailed`: `"Purchase failed"`
- `paywall.tryAgain`: `"Please try again."`
- `paywall.restored`: `"Restored!"`
- `paywall.restoredMsg`: `"Pro features are now unlocked."`
- `paywall.nothingToRestore`: `"Nothing to restore"`
- `paywall.nothingToRestoreMsg`: `"No previous Pro purchase found for this account."`
- `paywall.restoreFailed`: `"Restore failed"`

**stats** (StatsScreen.tsx):
- `stats.player`: `"Player"`
- `stats.games`: `"Games"`
- `stats.total`: `"Total"`
- `stats.bestWin`: `"Best Win"`
- `stats.empty`: `"No finished games yet — stats appear after your first settled game."`

**settings** (SettingsScreen.tsx):
- `settings.pro`: `"⭐ Pro"`
- `settings.unlock`: `"Unlock Pro — $2.99"`
- `settings.general`: `"General"`
- `settings.language`: `"Language"`
- `settings.data`: `"Data"`
- `settings.manageContacts`: `"Manage Contacts"`
- `settings.exportAll`: `"Export All Games (CSV)"`
- `settings.backup`: `"Backup Data"`
- `settings.restore`: `"Restore Data"`
- `settings.version`: `"PokerSplitter v{{version}}"`
- `settings.restoreBackupTitle`: `"Restore backup"`
- `settings.restoreActiveGame`: `"You have a game in progress. Restoring will discard it. Continue?"`
- `settings.restoreConfirm`: `"This will replace all current data. Continue?"`
- `settings.restoreSuccess`: `"Restored"`
- `settings.restoreSuccessMsg`: `"Data has been restored successfully."`
- `settings.error`: `"Error"`
- `settings.couldNotRead`: `"Could not read the file."`
- `settings.invalidBackup`: `"Invalid backup"`
- `settings.languageChanged`: `"Language changed"`
- `settings.languageChangedMsg`: `"The app will restart to apply the new language."`

**gameDetail** (GameDetailScreen.tsx):
- `gameDetail.players`: `"Players · {{date}}"`
- `gameDetail.settlement`: `"Settlement"`
- `gameDetail.exportCsv`: `"Export CSV"`

**contacts** (ContactsScreen.tsx):
- `contacts.noContacts`: `"No contacts saved yet."`
- `contacts.addContact`: `"+ Add Contact"`
- `contacts.editContact`: `"Edit Contact"`
- `contacts.newContact`: `"New Contact"`
- `contacts.namePlaceholder`: `"Name *"`
- `contacts.phonePlaceholder`: `"Phone (e.g. +972501234567)"`
- `contacts.nameRequired`: `"Name required"`
- `contacts.deleteTitle`: `"Delete contact"`
- `contacts.deleteMsg`: `"Remove this contact?"`
- `contacts.invalidPhone`: `"Enter a valid phone number (e.g. +972501234567)"`

**contactPicker** (ContactPickerModal.tsx):
- `contactPicker.title`: `"Select Contact"`
- `contactPicker.search`: `"Search contacts…"`
- `contactPicker.noContacts`: `"No contacts saved — add them in Settings."`
- `contactPicker.noMatches`: `"No matches."`

**player** (PlayerRow.tsx):
- `player.in`: `"In: ${{amount}}"`
- `player.rebuy_one`: `"{{count}} rebuy"`
- `player.rebuy_other`: `"{{count}} rebuys"`
- `player.out`: `"Out"`
- `player.rebuyBtn`: `"Rebuy"`
- `player.cashOutBtn`: `"Cash Out"`

**amount** (AmountModal.tsx):
- `amount.placeholder`: `"Enter amount"`

**whatsapp** (whatsapp.ts):
- `whatsapp.title`: `"🃏 Poker Night Results"`
- `whatsapp.noTransfers`: `"No transfers needed — everyone broke even!"`
- `whatsapp.totalPot`: `"Total pot: ${{amount}}"`
- `whatsapp.transferLine`: `"{{from}} {{arrow}} {{to}}: ${{amount}}"`
- `whatsapp.personalMsg`: `"Hey {{from}} 🃏 Poker night is settled!\nYou owe {{to}} ${{amount}}."`

- [ ] **Step 2: Commit**

```bash
git add src/i18n/locales/en.json
git commit -m "feat: add English translation file with all extracted strings"
```

---

### Task 3: Create Hebrew Translation File

**Files:**
- Create: `src/i18n/locales/he.json`

- [ ] **Step 1: Create the Hebrew translations file**

Create `src/i18n/locales/he.json` with all the same keys as `en.json`, translated to Hebrew. Key differences:
- `common.arrow`: `"←"` (reversed for RTL)
- All user-facing text in Hebrew
- Pluralization: Hebrew uses `_one` and `_other` suffixes (same as English for i18next)
- Keep `$` currency symbol as-is (currency localization is out of scope)
- Keep emoji characters as-is

- [ ] **Step 2: Commit**

```bash
git add src/i18n/locales/he.json
git commit -m "feat: add Hebrew translation file"
```

---

### Task 4: Create i18n Initialization Module

**Files:**
- Modify: `src/storage.ts` (export the storage instance)
- Create: `src/i18n/index.ts`

- [ ] **Step 1: Export the MMKV storage instance from storage.ts**

In `src/storage.ts`, change `const storage = createMMKV();` to `export const storage = createMMKV();` so the i18n modules can reuse the same instance instead of creating new ones.

- [ ] **Step 2: Create the i18n init file**

Create `src/i18n/index.ts`:

**Important:** Do NOT create a new MMKV instance. The existing `src/storage.ts` already creates one. First, add a named export for the storage instance in `src/storage.ts`:

Add `export { storage };` (or change `const storage` to `export const storage`) in `src/storage.ts` so the i18n modules can reuse the same instance.

Then in `src/i18n/index.ts`:

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import { getLocales } from 'expo-localization';
import { I18nManager } from 'react-native';
import { storage } from '../storage';
import en from './locales/en.json';
import he from './locales/he.json';

const USER_LANGUAGE_KEY = 'userLanguage';

// Resolve language: MMKV override > device locale > English fallback
function resolveLanguage(): 'en' | 'he' {
  const stored = storage.getString(USER_LANGUAGE_KEY);
  if (stored === 'en' || stored === 'he') return stored;

  const deviceLocale = getLocales()[0]?.languageCode ?? 'en';
  return deviceLocale.startsWith('he') ? 'he' : 'en';
}

const language = resolveLanguage();

// Set RTL synchronously before any component renders
I18nManager.allowRTL(true);
I18nManager.forceRTL(language === 'he');

i18n.use(initReactI18next).init({
  resources: {
    en: { translation: en },
    he: { translation: he },
  },
  lng: language,
  fallbackLng: 'en',
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **Step 3: Commit**

```bash
git add src/storage.ts src/i18n/index.ts
git commit -m "feat: add i18n initialization with locale detection and RTL setup"
```

---

### Task 5: Create Language Change Helper

**Files:**
- Create: `src/i18n/changeLanguage.ts`

- [ ] **Step 1: Create the changeLanguage helper**

Create `src/i18n/changeLanguage.ts`:

```typescript
import { Alert, I18nManager } from 'react-native';
import { storage } from '../storage';
import i18n from './index';
import RNRestart from 'react-native-restart';

const USER_LANGUAGE_KEY = 'userLanguage';

export type AppLanguage = 'en' | 'he';

export function getCurrentLanguage(): AppLanguage {
  return (i18n.language as AppLanguage) ?? 'en';
}

export function changeAppLanguage(newLang: AppLanguage): void {
  if (newLang === getCurrentLanguage()) return;

  storage.set(USER_LANGUAGE_KEY, newLang);
  i18n.changeLanguage(newLang);
  I18nManager.forceRTL(newLang === 'he');

  Alert.alert(
    i18n.t('settings.languageChanged'),
    i18n.t('settings.languageChangedMsg'),
    [{ text: 'OK', onPress: () => RNRestart.restart() }],
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/i18n/changeLanguage.ts
git commit -m "feat: add changeAppLanguage helper with RTL toggle and restart"
```

---

### Task 6: Create useTranslatedTitle Hook

**Files:**
- Create: `src/hooks/useTranslatedTitle.ts`

- [ ] **Step 1: Create the hook**

Create `src/hooks/useTranslatedTitle.ts`:

```typescript
import { useEffect } from 'react';
import { useTranslation } from 'react-i18next';
import { useNavigation } from '@react-navigation/native';

/**
 * Sets the navigation header title to a translated string.
 * Call from any screen: useTranslatedTitle('nav.gameSetup')
 */
export function useTranslatedTitle(key: string): void {
  const { t } = useTranslation();
  const navigation = useNavigation();

  useEffect(() => {
    navigation.setOptions({ title: t(key) });
  }, [t, key, navigation]);
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useTranslatedTitle.ts
git commit -m "feat: add useTranslatedTitle hook for translated navigation headers"
```

---

### Task 7: Wire i18n Init into App.tsx

**Files:**
- Modify: `App.tsx`

- [ ] **Step 1: Import i18n at the top of App.tsx**

Add `import './src/i18n';` as the very first import in `App.tsx` (after `import 'react-native-get-random-values'`). This ensures RTL is set before any component renders.

Remove the hardcoded `title` strings from screen options — screens will set their own titles via `useTranslatedTitle`. For screens that don't use the hook, keep the title as a fallback.

The `HeaderBtn` label `"Stats"` should use `t()` — but since `HeaderBtn` is not inside a component that can use `useTranslation` in the options function, convert the `headerRight` to use translated labels. The simplest approach: since `App` is a component, call `useTranslation()` inside `App` and pass `t` values to options.

```typescript
import './src/i18n';
// ... existing imports ...
import { useTranslation } from 'react-i18next';

export default function App() {
  const { t } = useTranslation();

  return (
    // ... existing JSX, but update screen options:
    // Home: options title = t('nav.home'), headerRight labels translated
    // Other screens: remove static titles (useTranslatedTitle handles them)
    // Paywall: keep headerShown: false
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add App.tsx
git commit -m "feat: wire i18n initialization into App.tsx, translate nav titles"
```

## Chunk 2: Screen Translations — Home, Setup, ActiveGame, FinalChipCount

### Task 8: Translate HomeScreen

**Files:**
- Modify: `src/screens/HomeScreen.tsx`

- [ ] **Step 1: Add useTranslation and replace all hardcoded strings**

Add `import { useTranslation } from 'react-i18next';` and `import i18n from '../i18n';`.

Call `const { t } = useTranslation();` inside the component.

Add `useTranslatedTitle('nav.home');` — but HomeScreen's title is set in App.tsx, so skip the hook here. The App.tsx options already handle it.

Replace strings:
- `"No games yet. Start one!"` → `t('home.noGames')`
- `"+ New Game"` → `t('home.newGame')`
- `` `${games.length}/${FREE_GAME_LIMIT} games used` `` → `t('home.gamesUsed', { count: games.length, limit: FREE_GAME_LIMIT })`
- `` `${FREE_GAME_LIMIT}/${FREE_GAME_LIMIT} games used — upgrade to add more` `` → `t('home.gamesUsedMax', { limit: FREE_GAME_LIMIT })`
- `'Active'` → `t('home.active')`
- `'Finished'` → `t('home.finished')`
- `` `${playerCount} players` `` → `t('home.players', { count: playerCount })`
- `new Date(item.date).toLocaleDateString()` → `new Date(item.date).toLocaleDateString(i18n.language === 'he' ? 'he-IL' : 'en-US')`

For the chevron `›`, leave as-is — it flips automatically with RTL layout.

Add `writingDirection: 'ltr'` to any Text component displaying currency amounts (none in HomeScreen's current view, but the meta line has no currency).

- [ ] **Step 2: Commit**

```bash
git add src/screens/HomeScreen.tsx
git commit -m "feat: translate HomeScreen strings to i18n keys"
```

---

### Task 9: Translate GameSetupScreen

**Files:**
- Modify: `src/screens/GameSetupScreen.tsx`

- [ ] **Step 1: Add useTranslation and useTranslatedTitle, replace strings**

Add imports: `useTranslation` from `react-i18next`, `useTranslatedTitle` from `../hooks/useTranslatedTitle`.

Call `const { t } = useTranslation();` and `useTranslatedTitle('nav.gameSetup');`.

Replace strings:
- `"Game name (optional)"` placeholder → `t('setup.gameName')`
- `"Player name"` placeholder → `t('setup.playerName')`
- `"Buy-in $"` placeholder → `t('setup.buyIn')`
- `"+ Add Player"` → `t('setup.addPlayer')`
- `"Start Game"` → `t('setup.startGame')`
- `Alert.alert('Need at least 2 players')` → `Alert.alert(t('setup.needPlayers'))`
- `Alert.alert('All players need a name')` → `Alert.alert(t('setup.needNames'))`
- `Alert.alert('All buy-in amounts must be positive numbers')` → `Alert.alert(t('setup.needBuyIn'))`
- `Alert.alert('Duplicate names', 'All players must have unique names')` → `Alert.alert(t('setup.duplicateNames'), t('setup.duplicateNamesMsg'))`

RTL style fix: Replace `marginRight: 8` with `marginEnd: 8` in `nameInput` style. Replace `marginRight: 4` with `marginEnd: 4` in `amountInput` and `contactBtn` styles.

- [ ] **Step 2: Commit**

```bash
git add src/screens/GameSetupScreen.tsx
git commit -m "feat: translate GameSetupScreen, fix directional margins for RTL"
```

---

### Task 10: Translate ActiveGameScreen

**Files:**
- Modify: `src/screens/ActiveGameScreen.tsx`

- [ ] **Step 1: Add useTranslation and replace strings**

Add `import { useTranslation } from 'react-i18next';`.

Call `const { t } = useTranslation();`.

Replace strings:
- `navigation.setOptions({ title: \`Pot: $\${pot(found).toFixed(2)}\` })` → `navigation.setOptions({ title: t('activeGame.pot', { amount: pot(found).toFixed(2) }) })`
- Same for the second `setOptions` call inside `addTransaction`
- `'Rebuy'` (modal title) → `t('activeGame.rebuy')`
- `'Cash Out'` (modal title) → `t('activeGame.cashOut')`
- `'End Game'` alert title → `t('activeGame.endGame')`
- `'Are you sure you want to end the game?'` → `t('activeGame.endGameConfirm')`
- `'End Game'` destructive button text in the alert → `t('activeGame.endGame')` (same key)

Note: The `promptAmount` function takes a `title` param — callers should pass the already-translated string: `promptAmount(t('activeGame.rebuy'), ...)` and `promptAmount(t('activeGame.cashOut'), ...)`.
- `'Enter amount:'` → `t('activeGame.enterAmount')`
- `'Enter a valid positive amount'` → `t('activeGame.invalidAmount')`
- `'Cancel'` → `t('common.cancel')`
- `'Confirm'` → `t('common.confirm')`

- [ ] **Step 2: Commit**

```bash
git add src/screens/ActiveGameScreen.tsx
git commit -m "feat: translate ActiveGameScreen strings"
```

---

### Task 11: Translate FinalChipCountScreen

**Files:**
- Modify: `src/screens/FinalChipCountScreen.tsx`

- [ ] **Step 1: Add useTranslation and useTranslatedTitle, replace strings**

Add imports. Call `useTranslatedTitle('nav.finalChipCount');`.

Replace strings:
- `` `Expected total: $${expectedChips().toFixed(2)}` `` → `t('finalChipCount.expectedTotal', { amount: expectedChips().toFixed(2) })`
- `"Chips $"` placeholder → `t('finalChipCount.chipsPlaceholder')`
- `"Calculate Settlement"` → `t('finalChipCount.calculate')`
- `'Enter chip counts'` → `t('finalChipCount.enterChipCounts')`
- `` `Missing or invalid amount for ${p.name}` `` → `t('finalChipCount.missingAmount', { name: p.name })`
- `'Chip count mismatch'` → `t('finalChipCount.mismatch')`
- The mismatch message → `t('finalChipCount.mismatchMsg', { entered: (entered / 100).toFixed(2), expected: (expected / 100).toFixed(2) })`

RTL: Add `writingDirection: 'ltr'` to the `hint` style (displays currency in "Expected total: $...") and to the `input` style (user enters currency amounts).

- [ ] **Step 2: Commit**

```bash
git add src/screens/FinalChipCountScreen.tsx
git commit -m "feat: translate FinalChipCountScreen strings, add LTR for currency"
```

## Chunk 3: Screen Translations — Settlement, Paywall, Stats, Settings

### Task 12: Translate SettlementScreen

**Files:**
- Modify: `src/screens/SettlementScreen.tsx`

- [ ] **Step 1: Add useTranslation and useTranslatedTitle, replace strings**

Add imports. Call `useTranslatedTitle('nav.settlement');`.

Replace strings in the main component:
- `"No transfers needed!"` → `t('settlement.noTransfersNeeded')`
- `"Everyone broke even."` → `t('settlement.everyoneBrokeEven')`
- `` `Total pot $${pot.toFixed(2)}` `` → `t('settlement.totalPot', { amount: pot.toFixed(2) })`
- The transfer count header → `t('settlement.transferCount', { count: transfers.length, pot: pot.toFixed(2) })`
- `"Share Results"` → `t('settlement.shareResults')`
- `"Share to WhatsApp"` → `t('settlement.shareWhatsApp')`
- `"Message Players"` → `t('settlement.messagePlayers')`
- `"WhatsApp not installed"` → `t('settlement.whatsappNotInstalled')`
- `"Done"` → `t('common.done')`

Replace the `handleShare` function's message construction to use `t()` keys:
- The title `"Poker Settlement"` → `t('settlement.shareTitle')`
- Each transfer line `${t.from} → ${t.to}: $${t.amount.toFixed(2)}` → `t('settlement.shareTransferLine', { from: t.from, arrow: t('common.arrow'), to: t.to, amount: t.amount.toFixed(2) })`
- `"No transfers needed — everyone broke even!"` → `t('settlement.noTransfersShareLine')`
- `"Total pot: $${pot.toFixed(2)}"` → `t('settlement.totalPot', { amount: pot.toFixed(2) })`

RTL: Add `writingDirection: 'ltr'` to the `header` style (displays "Total pot $..." currency) and to the `evenText`/`evenSub` styles if they show currency amounts.

Replace strings in `MessagePlayersModal`:
- `"Message Players"` title → `t('settlement.messagePlayers')`
- `"Done"` → `t('common.done')`
- `` `${t.from} owes ${t.to} $${t.amount.toFixed(2)}` `` → `t('settlement.owes', { from: t.from, to: t.to, amount: t.amount.toFixed(2) })`
- `"Send on WhatsApp"` → `t('settlement.sendWhatsApp')`
- The hint text → `t('settlement.contactHint', { name: t.from })`

Note: The `MessagePlayersModal` is a non-exported function component in the same file. It needs `t` passed as a prop or to call `useTranslation()` itself (preferred since it's a React component).

- [ ] **Step 2: Commit**

```bash
git add src/screens/SettlementScreen.tsx
git commit -m "feat: translate SettlementScreen and MessagePlayersModal"
```

---

### Task 13: Translate PaywallScreen

**Files:**
- Modify: `src/screens/PaywallScreen.tsx`

- [ ] **Step 1: Add useTranslation and replace strings**

Add `import { useTranslation } from 'react-i18next';`.
Call `const { t } = useTranslation();`.

Replace the `FEATURES` array — move it inside the component so `t()` is available, or keep it outside and compute translated features inside:

```typescript
const features = [
  t('paywall.feature.unlimitedGames'),
  t('paywall.feature.contacts'),
  t('paywall.feature.whatsapp'),
  t('paywall.feature.stats'),
  t('paywall.feature.csv'),
  t('paywall.feature.backup'),
];
```

Replace strings:
- `"Unlock PokerSplitter Pro"` → `t('paywall.title')`
- `"One-time purchase · No subscription"` → `t('paywall.subtitle')`
- `"Processing…"` → `t('paywall.processing')`
- `"Unlock Pro — $2.99"` → `t('paywall.unlock')`
- `"Restore Purchase"` → `t('paywall.restore')`
- `'Purchase failed'` → `t('paywall.purchaseFailed')`
- `e.message ?? 'Please try again.'` → `e.message ?? t('paywall.tryAgain')`
- `'Restored!'` → `t('paywall.restored')`
- `'Pro features are now unlocked.'` → `t('paywall.restoredMsg')`
- `'Nothing to restore'` → `t('paywall.nothingToRestore')`
- `'No previous Pro purchase found for this account.'` → `t('paywall.nothingToRestoreMsg')`
- `'Restore failed'` → `t('paywall.restoreFailed')`

RTL style fixes:
- Replace `marginRight: 10` in `check` style with `marginEnd: 10`.
- Replace `right: 16` in `closeBtn` style with `end: 16` (or use `start` if needed) so the close button appears on the correct side in RTL.

- [ ] **Step 2: Commit**

```bash
git add src/screens/PaywallScreen.tsx
git commit -m "feat: translate PaywallScreen, fix directional margin for RTL"
```

---

### Task 14: Translate StatsScreen

**Files:**
- Modify: `src/screens/StatsScreen.tsx`

- [ ] **Step 1: Add useTranslation and useTranslatedTitle, replace strings**

Add imports. Call `useTranslatedTitle('nav.stats');`.

Replace strings:
- `"Player"` → `t('stats.player')`
- `"Games"` → `t('stats.games')`
- `"Total"` → `t('stats.total')`
- `"Best Win"` → `t('stats.bestWin')`
- The empty state text → `t('stats.empty')`

Add `writingDirection: 'ltr'` to the `col` style for currency amounts (the `formatNet` function returns `$`-prefixed strings).

- [ ] **Step 2: Commit**

```bash
git add src/screens/StatsScreen.tsx
git commit -m "feat: translate StatsScreen strings"
```

---

### Task 15: Translate SettingsScreen + Add Language Picker

**Files:**
- Modify: `src/screens/SettingsScreen.tsx`

- [ ] **Step 1: Add useTranslation, useTranslatedTitle, and language picker imports**

Add imports:
```typescript
import { useTranslation } from 'react-i18next';
import { useTranslatedTitle } from '../hooks/useTranslatedTitle';
import { changeAppLanguage, getCurrentLanguage, AppLanguage } from '../i18n/changeLanguage';
```

Call `useTranslatedTitle('nav.settings');`.

- [ ] **Step 2: Replace all hardcoded strings with t() calls**

- `"⭐ Pro"` → `t('settings.pro')`
- `"Unlock Pro — $2.99"` → `t('settings.unlock')`
- `"Data"` → `t('settings.data')`
- `"Manage Contacts"` → `t('settings.manageContacts')` (append `!isPro && ' 🔒'` logic stays)
- `"Export All Games (CSV)"` → `t('settings.exportAll')` + lock icon
- `"Backup Data"` → `t('settings.backup')` + lock icon
- `"Restore Data"` → `t('settings.restore')` + lock icon
- `` `PokerSplitter v${version}` `` → `t('settings.version', { version })`
- All alert strings in `handleRestore` → use corresponding `t()` keys
- `'Error'` → `t('settings.error')`
- `'Could not read the file.'` → `t('settings.couldNotRead')`
- `'Invalid backup'` → `t('settings.invalidBackup')`
- `'Restore backup'` → `t('settings.restoreBackupTitle')`
- `'Cancel'` → `t('common.cancel')`
- `'Restore'` → `t('settings.restore')`

- [ ] **Step 3: Add language picker section**

Add a "General" section above the "Data" section:

```tsx
const LANGUAGE_NAMES: Record<AppLanguage, string> = { en: 'English', he: 'עברית' };

// Inside the return JSX, before the Data section:
<Text style={styles.sectionTitle}>{t('settings.general')}</Text>
<TouchableOpacity
  style={styles.row}
  onPress={() => {
    const current = getCurrentLanguage();
    const options = (['en', 'he'] as AppLanguage[]).map(lang => ({
      text: LANGUAGE_NAMES[lang],
      onPress: () => changeAppLanguage(lang),
    }));
    Alert.alert(t('settings.language'), undefined, [
      ...options,
      { text: t('common.cancel'), style: 'cancel' },
    ]);
  }}
>
  <Text style={styles.rowText}>{t('settings.language')}</Text>
  <View style={{ flexDirection: 'row', alignItems: 'center' }}>
    <Text style={{ color: '#777', marginEnd: 8 }}>{LANGUAGE_NAMES[getCurrentLanguage()]}</Text>
    <Text style={styles.chevron}>›</Text>
  </View>
</TouchableOpacity>
```

- [ ] **Step 4: Commit**

```bash
git add src/screens/SettingsScreen.tsx
git commit -m "feat: translate SettingsScreen, add language picker with RTL restart"
```

## Chunk 4: Remaining Screens

### Task 16: Translate GameDetailScreen

**Files:**
- Modify: `src/screens/GameDetailScreen.tsx`

- [ ] **Step 1: Add useTranslation and replace strings**

Add `import { useTranslation } from 'react-i18next';` and `import i18n from '../i18n';`.

Call `const { t } = useTranslation();`.

Replace strings:
- `navigation.setOptions({ title: found.name ?? new Date(found.date).toLocaleDateString() })` — add locale to date: `new Date(found.date).toLocaleDateString(i18n.language === 'he' ? 'he-IL' : 'en-US')`
- `` `Players · ${gameDate}` `` → `t('gameDetail.players', { date: gameDate })` (also localize the `gameDate` variable with locale)
- `'Settlement'` → `t('gameDetail.settlement')`
- `` `Export CSV ${!isPro && '🔒'}` `` → `t('gameDetail.exportCsv')` + lock icon logic

RTL: Add `writingDirection: 'ltr'` to the `net` style (displays currency amounts like `+$45.00` / `-$12.00`).

- [ ] **Step 2: Commit**

```bash
git add src/screens/GameDetailScreen.tsx
git commit -m "feat: translate GameDetailScreen, localize dates, add LTR for currency"
```

---

### Task 17: Translate ContactsScreen

**Files:**
- Modify: `src/screens/ContactsScreen.tsx`

- [ ] **Step 1: Add useTranslation and useTranslatedTitle, replace strings**

Add imports. Call `useTranslatedTitle('nav.contacts');`.

Replace strings:
- `"No contacts saved yet."` → `t('contacts.noContacts')`
- `"+ Add Contact"` → `t('contacts.addContact')`
- `editing ? 'Edit Contact' : 'New Contact'` → `editing ? t('contacts.editContact') : t('contacts.newContact')`
- `"Name *"` placeholder → `t('contacts.namePlaceholder')`
- `"Phone (e.g. +972501234567)"` placeholder → `t('contacts.phonePlaceholder')`
- `'Name required'` alert → `t('contacts.nameRequired')`
- `'Delete contact'` → `t('contacts.deleteTitle')`
- `'Remove this contact?'` → `t('contacts.deleteMsg')`
- `'Cancel'` → `t('common.cancel')`
- `'Delete'` → `t('common.delete')`
- `'Save'` → `t('common.save')`
- The phone validation error message → `t('contacts.invalidPhone')`

- [ ] **Step 2: Commit**

```bash
git add src/screens/ContactsScreen.tsx
git commit -m "feat: translate ContactsScreen strings"
```

## Chunk 5: Component Translations + WhatsApp + Verification

### Task 18: Translate PlayerRow Component

**Files:**
- Modify: `src/components/PlayerRow.tsx`

- [ ] **Step 1: Add useTranslation and replace strings**

Add `import { useTranslation } from 'react-i18next';`.
Call `const { t } = useTranslation();`.

Replace strings:
- `` `In: $${inAmount.toFixed(2)}` `` → `t('player.in', { amount: inAmount.toFixed(2) })`
- The rebuy count suffix: `` ` · ${rebuyCount} rebuy${rebuyCount > 1 ? 's' : ''}` `` → `` ` · ${t('player.rebuy', { count: rebuyCount })}` ``
- `"Rebuy"` button → `t('player.rebuyBtn')`
- `"Cash Out"` button → `t('player.cashOutBtn')`
- `"Out"` badge → `t('player.out')`

Add `writingDirection: 'ltr'` to the meta text style for the currency amount portion.

- [ ] **Step 2: Commit**

```bash
git add src/components/PlayerRow.tsx
git commit -m "feat: translate PlayerRow component strings"
```

---

### Task 19: Translate AmountModal Component

**Files:**
- Modify: `src/components/AmountModal.tsx`

- [ ] **Step 1: Add useTranslation and replace strings**

Add `import { useTranslation } from 'react-i18next';`.
Call `const { t } = useTranslation();`.

Replace strings:
- `"Enter amount"` placeholder → `t('amount.placeholder')`
- `"Cancel"` → `t('common.cancel')`
- `"Confirm"` → `t('common.confirm')`

- [ ] **Step 2: Commit**

```bash
git add src/components/AmountModal.tsx
git commit -m "feat: translate AmountModal component strings"
```

---

### Task 20: Translate TransferRow Component

**Files:**
- Modify: `src/components/TransferRow.tsx`

- [ ] **Step 1: Add useTranslation and replace arrow**

Add `import { useTranslation } from 'react-i18next';`.
Call `const { t } = useTranslation();`.

Replace the arrow:
- `" → "` → `{` ${t('common.arrow')} `}` (renders `→` in English, `←` in Hebrew)

Add `writingDirection: 'ltr'` to the `amount` style for the currency display.

- [ ] **Step 2: Commit**

```bash
git add src/components/TransferRow.tsx
git commit -m "feat: translate TransferRow, use directional arrow for RTL"
```

---

### Task 21: Translate ContactPickerModal Component

**Files:**
- Modify: `src/components/ContactPickerModal.tsx`

- [ ] **Step 1: Add useTranslation and replace strings**

Add `import { useTranslation } from 'react-i18next';`.
Call `const { t } = useTranslation();`.

Replace strings:
- `"Select Contact"` → `t('contactPicker.title')`
- `"Cancel"` → `t('common.cancel')`
- `"Search contacts…"` placeholder → `t('contactPicker.search')`
- `"No contacts saved — add them in Settings."` → `t('contactPicker.noContacts')`
- `"No matches."` → `t('contactPicker.noMatches')`

- [ ] **Step 2: Commit**

```bash
git add src/components/ContactPickerModal.tsx
git commit -m "feat: translate ContactPickerModal component strings"
```

---

### Task 22: Translate WhatsApp Utils

**Files:**
- Modify: `src/utils/whatsapp.ts`

- [ ] **Step 1: Update whatsapp.ts to use i18n translations**

Import i18n directly (this is not a React component, so no hook):

```typescript
import i18n from '../i18n';
```

Update `buildSummaryURL`:
```typescript
export function buildSummaryURL(transfers: Transfer[], potTotal: number): string {
  const t = i18n.t.bind(i18n);
  const arrow = t('common.arrow');
  let body: string;
  if (transfers.length === 0) {
    body = t('whatsapp.noTransfers');
  } else {
    const lines = transfers.map(tr =>
      t('whatsapp.transferLine', { from: tr.from, arrow, to: tr.to, amount: tr.amount.toFixed(2) })
    );
    body = lines.join('\n');
  }
  const text = `${t('whatsapp.title')}\n\n${body}\n\n${t('whatsapp.totalPot', { amount: potTotal.toFixed(2) })}`;
  return `whatsapp://send?text=${encodeURIComponent(text)}`;
}
```

Update `buildTransferURL`:
```typescript
export function buildTransferURL(phone: string, from: string, to: string, amount: number): string {
  const t = i18n.t.bind(i18n);
  const text = t('whatsapp.personalMsg', { from, to, amount: amount.toFixed(2) });
  return `whatsapp://send?phone=${encodeURIComponent(phone)}&text=${encodeURIComponent(text)}`;
}
```

- [ ] **Step 2: Commit**

```bash
git add src/utils/whatsapp.ts
git commit -m "feat: translate WhatsApp message templates"
```

---

### Task 23: Fix Test Setup for i18n

**Files:**
- Create: `__tests__/setup-i18n.ts`
- Modify: `jest.config.js`

- [ ] **Step 1: Create i18n test setup file**

Create `__tests__/setup-i18n.ts` that initializes i18next with English translations for the test environment:

```typescript
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import en from '../src/i18n/locales/en.json';

i18n.use(initReactI18next).init({
  resources: { en: { translation: en } },
  lng: 'en',
  fallbackLng: 'en',
  interpolation: { escapeValue: false },
});
```

- [ ] **Step 2: Add setup file to jest config**

Update `jest.config.js`:

```javascript
module.exports = {
  preset: 'jest-expo',
  setupFiles: ['./__tests__/setup-i18n.ts'],
};
```

This ensures `i18n.t()` calls in `whatsapp.ts` and other modules resolve to English strings during tests, so existing test assertions continue to pass.

- [ ] **Step 3: Commit**

```bash
git add __tests__/setup-i18n.ts jest.config.js
git commit -m "test: add i18n jest setup so existing tests work with translated strings"
```

---

### Task 24: Final Verification

- [ ] **Step 1: TypeScript compilation check**

```bash
npx tsc --noEmit
```

Expected: No errors. Fix any type errors related to the i18n changes.

- [ ] **Step 2: Run tests**

```bash
npm test
```

Expected: Tests pass. If tests reference hardcoded strings, update them to match translated output or mock `useTranslation`.

- [ ] **Step 3: Manual verification checklist**

Start the app and verify:
1. App starts in English (or Hebrew based on device locale)
2. All screens show translated strings (no raw keys visible)
3. Settings → Language picker works, shows "English" / "עברית"
4. Changing language shows restart alert
5. After restart, app is in the new language
6. Hebrew mode: full RTL layout (back button on right, rows mirrored)
7. Currency amounts display correctly in RTL ($150.00, not 00.150$)
8. Dates are localized
9. Arrows in TransferRow point correctly per language direction
10. WhatsApp share messages are in the correct language

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "fix: address any remaining i18n issues from verification"
```

(Only if there were fixes needed in step 1-3.)
